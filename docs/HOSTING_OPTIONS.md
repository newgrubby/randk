# HOSTING_OPTIONS — размещение в российской инфраструктуре

Проект остаётся на Next.js. Поддерживаются два режима сборки, переключаемые
переменной `BUILD_TARGET` в `next.config.ts`.

---

## Краткий вывод

**Рекомендуется вариант 1 — статическая сборка.** После удаления приёма заявок
в проекте не осталось ничего, что требует постоянно работающего Node.js-сервера,
поэтому сайт можно разместить на обычном Linux-хостинге REG.RU без потери качества.

---

## Проверка пригодности к статическому экспорту

| Условие из задания                      | Статус | Комментарий                                                   |
| --------------------------------------- | ------ | ------------------------------------------------------------- |
| Дизайн не ухудшается                    | ✅     | Макет, палитра, типографика и анимации не менялись            |
| Страницы работают                       | ✅     | 28 HTML-страниц, все отвечают 200, у каждой один `h1`         |
| Фильтры работают                        | ✅     | Каталог фильтруется на клиенте, карточки есть в исходном HTML |
| Модальные окна работают                 | ✅     | Контактное окно, выбор города и офиса, копирование номера     |
| Анимации работают                       | ✅     | `motion` работает на клиенте, без JS есть noscript-фолбэк     |
| SEO сохраняется                         | ✅     | title, description, canonical, OG, JSON-LD, sitemap, robots   |
| Изображения выводятся                   | ✅     | SVG-композиции отдаются как есть                              |
| Нет API-маршрутов                       | ✅     | `src/app/api` удалён вместе с приёмом заявок                  |
| Нет обязательного SSR                   | ✅     | Все страницы пререндерятся при сборке                         |
| Ничего не сломается на обычном хостинге | ✅     | Проверено на локальном статическом сервере                    |

**Что теряется при экспорте и как компенсировано:**

| Ограничение                                     | Компенсация                                                                                                              |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `redirects()` из `next.config` не работает      | Редиректы выполняет площадка: `.htaccess` на REG.RU, `vercel.json` на Vercel. Оба файла генерируются из одного источника |
| Оптимизатор изображений `next/image` недоступен | `images.unoptimized: true`; изображения — лёгкие SVG (1–2 КБ)                                                            |
| Нет серверных query-параметров                  | Фильтр `?age=` читается на клиенте, карточки есть в HTML                                                                 |

---

## Редиректы: один источник, два конфига

В статическом режиме Next.js не выполняет редиректы сам — это делает площадка,
и у каждой свой формат. Чтобы не поддерживать два списка вручную, оба конфига
генерируются из одного файла.

```
src/config/redirects.data.json     ← единственный источник, 39 правил
            │
            ├── public/.htaccess   → REG.RU и любой Apache (попадает в out/)
            ├── vercel.json        → Vercel (лежит в корне репозитория)
            └── next.config.ts     → только в режиме standalone
```

| Площадка                 | Кто выполняет редиректы | Файл                                     |
| ------------------------ | ----------------------- | ---------------------------------------- |
| REG.RU и обычный хостинг | Apache, `mod_rewrite`   | `public/.htaccess` → копируется в `out/` |
| Vercel                   | платформа               | `vercel.json` в корне репозитория        |
| VPS в режиме standalone  | сам Next.js             | `next.config.ts → redirects()`           |

### Команды

```bash
npm run redirects
```

Пересобирает `.htaccess` и `vercel.json`. Запускается автоматически перед `npm run build`.

```bash
npm test
```

Проверяет правила и синхронизацию: дубликаты, редиректы «сам на себя», циклы,
цепочки из нескольких переходов, завершающие слэши, существование целевых
маршрутов и совпадение содержимого источника с обоими конфигами. При
расхождении завершается с ненулевым кодом.

### Почему `vercel.json` лежит в репозитории

Vercel читает конфигурацию **до** запуска сборки. Файл, созданный во время
`npm run build`, был бы прочитан слишком поздно и не подействовал бы, поэтому
`vercel.json` коммитится, а генератор поддерживает его в актуальном состоянии.
Расхождение с источником поймает `npm test`.

### Завершающие слэши

В проекте включён `trailingSlash: true`: страницы лежат в `about/index.html`,
и адрес `/about` нормализуется в `/about/`.

Поэтому цели редиректов записаны сразу со слэшем — `/uchebnyy_process → /about/`.
Если бы целью был `/about`, получилась бы цепочка из двух переходов
(`/uchebnyy_process → /about → /about/`): лишний хоп, потеря веса ссылки при
переносе и замедление для посетителя.

Источники, наоборот, записаны без слэша; шаблон `^…/?$` в Apache и нормализация
Vercel принимают оба варианта написания старого адреса.

**Проверено локально:** все 39 правил приводят на конечную страницу ровно за
один переход, код 308, финальный ответ 200; несуществующий адрес отдаёт 404,
а не редирект.

---

## Вариант 1 — статическая сборка (REG.RU и любой обычный хостинг)

### Сборка

```bash
npm ci && npm run build
```

Результат — папка `out/` (около 5 МБ), включая `.htaccess` с 39 правилами редиректа.

### Публикация

1. Открыть файловый менеджер или FTP панели REG.RU.
2. Загрузить **содержимое** папки `out/` в корень сайта (обычно `public_html` или `www/<домен>`).
   Загружать нужно именно содержимое, а не саму папку.
3. Убедиться, что файл `.htaccess` загрузился — многие FTP-клиенты скрывают файлы,
   начинающиеся с точки. Включите отображение скрытых файлов.
4. Проверить, что открывается `https://randkcenter.ru/` и `https://randkcenter.ru/languages/german/`.
5. Проверить редирект: `https://randkcenter.ru/nemeckiy_yazyk` должен вести на `/languages/german`.

### Требования к хостингу

- Apache с включёнными `mod_rewrite`, `mod_headers`, `mod_expires` (стандартно для REG.RU);
- SSL-сертификат (Let's Encrypt подключается в панели бесплатно);
- PHP **не требуется**, Node.js на сервере **не требуется**.

### Обновление сайта

Пересобрать локально и заново загрузить содержимое `out/`. База данных отсутствует,
поэтому потерять данные при обновлении невозможно.

### Ограничение

Метаданные строятся при сборке, поэтому любое изменение контента требует
пересборки и повторной загрузки. Для сайта, который меняется несколько раз в год,
это не проблема.

---

## Вариант 2 — standalone (VPS или облачный Node.js-хостинг)

Нужен, если позже вернётся приём заявок, появится админ-панель или потребуется
серверная логика (например, актуальное расписание из внешнего источника).

### Сборка

```bash
npm ci && npm run build:standalone
```

Результат: `.next/standalone/` + `.next/static/` + `public/`.

### Запуск на сервере

```bash
NODE_ENV=production PORT=3000 node server.js
```

Процесс держать под `pm2` или systemd.

### Docker

```bash
docker build -t randk-center --build-arg NEXT_PUBLIC_SITE_URL=https://randkcenter.ru .
```

```bash
docker run -d -p 3000:3000 --env-file .env.production --restart unless-stopped randk-center
```

`Dockerfile` уже собирает образ в режиме standalone.

### Nginx перед приложением

```nginx
server {
    server_name randkcenter.ru www.randkcenter.ru;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### Подходящие площадки в России

- VPS REG.RU, Timeweb Cloud, Selectel, Yandex Cloud (Compute Cloud);
- Yandex Cloud Serverless Containers — при использовании Docker-образа.

⚠️ **Обычный PHP shared-хостинг для этого варианта не подходит:** нужен постоянно
работающий Node.js-процесс, которого на таких тарифах нет.

---

## Сравнение

| Критерий                | Статический экспорт             | Standalone                                       |
| ----------------------- | ------------------------------- | ------------------------------------------------ |
| Тип хостинга            | обычный shared                  | VPS / Node.js / Docker                           |
| Стоимость               | минимальная                     | выше                                             |
| Настройка               | загрузить папку                 | развернуть сервис, настроить прокси и автозапуск |
| Обновление контента     | пересборка + загрузка           | пересборка + перезапуск                          |
| Редиректы               | `.htaccess`                     | `next.config.ts`                                 |
| Оптимизация изображений | отключена                       | включена                                         |
| Отказоустойчивость      | статика почти не падает         | зависит от процесса                              |
| Приём заявок в будущем  | потребует перехода на вариант 2 | доступен сразу                                   |

---

## Смена режима

Переключение выполняется одной переменной и не требует правок кода:

```bash
BUILD_TARGET=standalone next build
```

или готовой командой:

```bash
npm run build:standalone
```

Список редиректов при этом остаётся единым — `src/config/redirects.data.json`.
В режиме standalone его читает `next.config.ts`, в статическом — генератор,
который собирает `.htaccess` и `vercel.json`. Разойтись они не могут:
синхронизацию проверяет `npm test`.

**Статический экспорт остаётся основным режимом.** Переключение на standalone
не требуется ни для REG.RU, ни для Vercel — редиректы на обеих площадках
работают на статике.
