# PROGRESS

**Статус:** структура сайта доработана, оба режима сборки проходят.
Ветка: `randk-structure-completion`.

---

## Что сохранено из утверждённого макета

Дизайн-концепция **не менялась**. Сохранены без изменений:

- палитра (тёплый молочный фон, графитовый текст, бордово-красный акцент);
- типографическая пара Playfair Display + Inter и вся шкала размеров;
- дизайн-токены в `globals.css`;
- композиция первого экрана (асимметрия 6/6, вынос изображения в край, каскад появления);
- логотип-леттеринг;
- форма и характер карточек, сетка, чередование фонов секций;
- характер анимаций и поддержка `prefers-reduced-motion`;
- секционные примитивы, кнопки, аккордеон, модальное окно, хлебные крошки, ленивая карта.

---

## Сделано

### Модель данных

- [x] `Office` — самостоятельная сущность: **4 офиса** вместо «филиал = город»
- [x] `City` — группа офисов, 3 города
- [x] `Language` — 6 активных + 5 заготовок (скрыты флагом)
- [x] `Program` — 7 направлений с собственными маршрутами
- [x] Флаги видимости `active` / `confirmed` / `showInNavigation` / `showInSitemap`
- [x] Адреса и телефоны офисов опубликованы, помечены как ожидающие подтверждения

### Приём заявок удалён

- [x] Удалены `api/lead`, `server/notifiers`, `lib/lead-schema`, `lib/utm`, все формы и квиз
- [x] Единое контактное окно: город → офис → телефон, звонок, копирование, VK, MAX, маршрут
- [x] Мобильная панель «Позвонить · Центры · Написать»
- [x] Все контактные действия работают без JavaScript (`tel:`, VK, карты — обычные ссылки)
- [x] Политика конфиденциальности переписана: сайт не собирает персональные данные

### Страницы — 28 HTML

- [x] `/languages` + 6 страниц языков
- [x] `/programs` + `/exams`, `/tutoring`, `/preschool`, `/development`, `/corporate`, `/speech-therapist`, `/psychologist`
- [x] `/centers` + 3 страницы городов (в Павловском Посаде — два развёрнутых блока офисов)
- [x] `/reviews`, `/cookies`
- [x] Обновлены `/`, `/about`, `/contacts`, `/privacy`, 404
- [x] Раздел преподавателей снят целиком: Информация о преподавателях на сайте не публикуется по решению клиента
- [x] Языки и направления собираются едиными шаблонами, а не копированием компонентов

### Главная

Порядок секций сохранён, добавлены два смысловых блока:

- [x] Каталог языков — раньше сайт читался как школа одного английского
- [x] Методика и подход
- [x] Сетка офисов показывает 4 карточки вместо 3
- [x] Первый экран: новый подзаголовок и кнопки «Выбрать направление» / «Выбрать центр»

### Изображения

- [x] Структура `public/images/{client,generated,languages,programs,ages,offices,ui}`
- [x] Композиции для 11 языков, 7 направлений, 4 возрастов
- [x] Карточки офисов с городом, адресом и линиями карты — фасады намеренно не рисуются
- [x] Документация в `IMAGE_ASSETS.md`

### SEO

- [x] Уникальные title и description на всех страницах, один `h1`
- [x] canonical, Open Graph, favicon, `manifest.webmanifest`
- [x] `LocalBusiness` на каждый физический офис (появится при подтверждении данных)
- [x] `EducationalOrganization`, `Course`, `BreadcrumbList`, `FAQPage`
- [x] `sitemap.xml` из контента с учётом флагов — 26 URL
- [x] 39 правил редиректа, единый источник для обоих режимов сборки

### Сборка и хостинг

- [x] Статический экспорт в `out/` — 28 страниц, 5,1 МБ, без Node.js на сервере
- [x] Автогенерация `.htaccess` с редиректами, заголовками и кэшированием
- [x] Режим standalone сохранён (`npm run build:standalone`), Dockerfile актуален
- [x] `docs/HOSTING_OPTIONS.md` с разбором обоих вариантов

---

## Проверено

| Проверка                                                               | Результат                             |
| ---------------------------------------------------------------------- | ------------------------------------- |
| `npm run typecheck`                                                    | без ошибок                            |
| `npm run lint`                                                         | без ошибок                            |
| `npm run build` (статический экспорт)                                  | 32 маршрута, 28 HTML, экспорт успешен |
| `npm run build:standalone`                                             | успешно, `server.js` создан           |
| 26 маршрутов: код 200, один `h1`, заполненный title                    | все прошли                            |
| Четыре адреса офисов на главной                                        | все присутствуют                      |
| Павловский Посад: «Офис 1 из 2» и «Офис 2 из 2», два разных телефона   | прошло                                |
| 4 разных номера в `tel:` в исходном HTML                               | прошло                                |
| 6 языков в каталоге, скрытые не выводятся                              | прошло                                |
| Отсутствие `<form>` и `<input>` на всех проверенных страницах          | прошло                                |
| VK выводится, MAX скрыт (ссылки нет)                                   | прошло                                |
| Логопед и психолог: страницы есть, в шапке нет                         | прошло                                |
| Контактное окно: переключение офиса и города, Escape                   | прошло                                |
| Горизонтальный скролл на 1920 / 1265 / 768 / 360                       | отсутствует                           |
| Мобильная панель: 3 кнопки 107×48 px, подвал не перекрыт (зазор 67 px) | прошло                                |
| Карточки каталога в исходном HTML (без JS)                             | 24 ссылки                             |
| Редиректы в `.htaccess`                                                | 39 правил, ключевые проверены         |

**Итог автопроверок:** 64 из 65; единственное расхождение — ошибка в ожидаемом
числе URL в моём тесте (26 фактических против 24 ожидаемых), сайт корректен.

---

## Не входит в объём

- CMS и админ-панель (контент правится в `src/content/`);
- приём и хранение заявок — удалён сознательно по требованию;
- личный кабинет, онлайн-оплата, бронирование мест;
- раздел новостей и блог;
- мультиязычность интерфейса.

---

## Дальнейшие шаги

### Блокирует запуск

1. Подтвердить адреса, телефоны и графики четырёх офисов
2. Добавить координаты и ссылки на Яндекс Карты — без них нет карты и `LocalBusiness`
3. Подтвердить состав языков и направлений, определиться с ценами
4. Получить реквизиты, отдать юридические тексты на проверку
5. Решить по логопеду и психологу: выводить ли в основное меню

### После запуска

6. Заменить временные изображения на фотографии офисов
7. Получить вектор логотипа
8. Собрать отзывы со ссылками на источник
9. Выбрать основное сообщество VK, добавить ссылку MAX
10. Подключить Яндекс Метрику
11. Сверить редиректы с Вебмастером перед переключением DNS

### Рекомендуемые улучшения

12. Включить дополнительные языки, если они реально ведутся — отдельные поисковые запросы
13. Фотогалерея офисов
14. Отдельные посадочные страницы «язык + город» для локального SEO
15. Замер Lighthouse на боевом домене после замены изображений
